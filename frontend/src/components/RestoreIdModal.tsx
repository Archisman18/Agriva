import { useState } from 'react';

// Stub retained for type compatibility if needed
export default function RestoreIdModal({ show }: { show: boolean; onClose: () => void }) {
  if (!show) return null;
  return null;
}
